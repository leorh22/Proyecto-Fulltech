<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar dispositivo</title>
    <link rel="stylesheet" href="/css/main.css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
    <link rel="icon" href="/icons/fulltech-logo.ico" type="image/x-icon">
</head>
<body>
    <% if(data) { %>
    <div class="site-nuevo-page">
        <div class="site-nuevo-form">
            <div class="exit-site-button">
                <a href="/site"><span class="material-symbols-outlined" style="font-size: 30px;">close</span></a>
            </div>
            <div class="site-form">
                <form action="/actualizarSite" method="post">
                    <input type="text" name="id" id="id" value="<%= data.id_dispositivo %>" hidden>
                    <div>
                        <label for="">DISPOSITIVO:</label>
                        <input type="text" name="disp" id="disp" value="<%= data.dispositivo %>">
                    </div>
                    <div>
                        <label for="">TIPO DE DISPOSITIVO:</label>
                        <input type="text" name="tipo_disp" id="tipo_disp" value="<%= data.tipo_dispositivo %>">
                    </div>
                    <div>
                        <label for="">MARCA:</label>
                        <input type="text" name="marca_disp" id="marca_disp" required value="<%= data.marca %>">
                    </div>
                    <div>
                        <label for="">MODELO:</label>
                        <input type="text" name="modelo_disp" id="modelo_disp" value="<%= data.modelo %>">
                    </div>
                    <div>
                        <label for="">NÚMERO DE SERIE:</label>
                        <input type="text" name="num_serie" id="num_serie" value="<%= data.no_serie %>">
                    </div>
                    <div>
                        <label for="">COMENTARIO:</label>
                        <input type="text" name="comentario_disp" id="comentario_disp" value="<%= data.comentario %>">
                    </div>
                    <div>
                        <label for="">ESTADO DEL DISPOSITIVO:</label>
                        <select name="estado_disp" id="estado_disp">
                            <option <%= data.estado_dispositivo=='1' ? 'selected' : '' %> value="1">ACTIVO</option>
                            <option  <%= data.estado_dispositivo=='0' ? 'selected' : '' %> value="0">BAJA</option>
                        </select>
                    </div>
                    <div>
                        <div class="site-guardar-button">
                            <button type="submit">ACTUALIZAR</button>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
    <% } %>
</body>
</html>