<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detalles de la licencia</title>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
    <link rel="stylesheet" href="/css/main.css">
    <link rel="icon" href="/icons/fulltech-logo.ico" type="image/x-icon">
</head>
<body>
    <div class="detalles-licencia-page">
        <div class="licencia-container">
            <div class="exit-licencia-detalles">
                <a href="/licencia"><span class="material-symbols-outlined" style="font-size: 30px;">close</span></a>
            </div>
            <div class="licencia-table">
                <table>
                    <% if(data) { %>
                        <tr><th>NOMBRE DE LA LICENCIA</th><td><%= data.nombre %></td></tr>
                        <tr><th>TIPO</th><td><%= data.tipo_licencia %></td></tr>
                        <tr><th>KEYCODE</th><td><%= data.keycode %></td></tr>
                        <tr><th>FECHA DE COMPRA</th><td><%= data.fecha_compra %></td></tr>
                        <tr><th>FECHA DE RENOVACIÓN</th><td><%= data.fecha_renovacion %></td></tr>
                        <tr><th>LICENCIA ACTIVA POR</th><td>
                            <% if(data.tiempo_restante <= 0) { %>
                                <b style="color: red;">SIN LICENCIA</b>
                            <% } else { %>
                                <b><%= data.tiempo_restante %></b> DÍAS RESTANTES
                                <% } %>
                        </td></tr>
                        <tr><th>PROVEEDOR</th><td><%= data.proveedor %></td></tr>
                        <tr><th>COMENTARIO</th><td><%= data.comentario %></td></tr>
                        <tr><th>EQUIPO ASIGNADO</th><td><%= data.no_serie %></td></tr>
                        <% } else { %>
                        <tr><td colspan="2">No se encontraron detalles para esta licencia</td></tr>
                        <% } %>
                </table>
            </div>
        </div>
    </div>
</body>
</html>